import { Category } from '../../types';
export default function FoodFilter({search,setSearch,categoryId,setCategoryId,categories}:{search:string;setSearch:(v:string)=>void;categoryId:string;setCategoryId:(v:string)=>void;categories:Category[]}){ return <div className="card flex flex-col md:flex-row gap-3"><input className="input" placeholder="Search foods" value={search} onChange={e=>setSearch(e.target.value)}/><select className="input" value={categoryId} onChange={e=>setCategoryId(e.target.value)}><option value="">All Categories</option>{categories.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></div> }
